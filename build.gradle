plugins {
    id "org.sonarqube" version "2.8"
}

subprojects {
//    apply plugin: "org.sonarqube"
    repositories {
        mavenCentral()
        maven {
            url "https://artifacts.alfresco.com/nexus/content/repositories/public/"
        }
    }
}

sonarqube {
    properties {
        property "sonar.projectKey", "speech4j_speech4j"
    }
}